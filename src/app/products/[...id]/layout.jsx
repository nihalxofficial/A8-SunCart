import React from 'react';

export const metadata = {
  title: "SunCart - Product Details",
};
const ProductDetailsLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default ProductDetailsLayout;