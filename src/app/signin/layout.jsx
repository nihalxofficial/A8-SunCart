import React from 'react';
export const metadata = {
  title: "SunCart - SignIn",
};
const SignInLayout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default SignInLayout;